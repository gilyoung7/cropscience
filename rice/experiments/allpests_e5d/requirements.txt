# Environment for the all-pest E5d run.
#
# CUDA build is pinned to cu118 on purpose. This host reports driver 525.60.11 / CUDA 12.0,
# and the cu121 wheels require >= 525.60.13 -- two patch releases newer than what is installed.
# cu118 is fully covered by any 525 driver, so it is the highest build that is guaranteed to
# load here. Do not bump to cu12x without first checking `nvidia-smi` on BOTH servers.
#
#   python3 -m venv .venv
#   .venv/bin/pip install -r rice/experiments/allpests_e5d/requirements.txt \
#       --extra-index-url https://download.pytorch.org/whl/cu118
#
--extra-index-url https://download.pytorch.org/whl/cu118

torch==2.5.1+cu118

numpy==1.26.4
pandas==2.2.3
scipy==1.14.1
scikit-learn==1.5.2
lightgbm==4.5.0
xgboost==2.1.3
matplotlib==3.9.2
PyYAML==6.0.2
wandb==0.18.7
